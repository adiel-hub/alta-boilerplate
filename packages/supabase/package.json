{
  "name": "@alta/supabase",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "gen-types": "npx supabase gen types typescript --local > src/types/database.types.ts",
    "db:start": "npx supabase start",
    "db:stop": "npx supabase stop",
    "db:reset": "npx supabase db reset",
    "db:migrate": "npx supabase migration up",
    "db:new-migration": "npx supabase migration new"
  },
  "devDependencies": {
    "@alta/typescript-config": "workspace:*",
    "supabase": "^2.0.0",
    "typescript": "^5.7.0"
  }
}
